/* =========================================================================
   MediShelf - static, frontend-only build.
   -------------------------------------------------------------------------
   There is no backend server or database anymore. All data is generated
   in the browser by mock-backend.js (seeded from mock-cases-data.js) and
   kept in this browser's localStorage, so this file is now just a marker
   for the app being fully static. Nothing here needs to change to deploy.
   ========================================================================= */
window.API_BASE = "";
